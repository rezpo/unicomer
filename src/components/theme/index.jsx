import { createTheme } from "@rneui/themed";

const theme = createTheme({
  colors: {
    primary: "#00aaff",
    secondary: "#ff0000",
    tertiary: "#ff00ff",
    quaternary: "#00ff00",
    quinary: "#0000ff",
    white: "#FFF",
    black: "#000",
  },
});

export default theme;
